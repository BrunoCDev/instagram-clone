import { StyleSheet } from "react-native";

export const styles = () =>
  StyleSheet.create({
    container: {
      flex: 1,
      height: 1000,
      backgroundColor: "white"
    },
    avatar: {
      height: 400,
      width: 360
    }
  });
