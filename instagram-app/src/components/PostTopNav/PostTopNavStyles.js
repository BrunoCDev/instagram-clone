import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    height: 50,
    flexDirection: "row"
  },
  avatar: {
    height: 30,
    width: 30,
    borderRadius: 30
  }
});
